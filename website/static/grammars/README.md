# TextMate grammars

Place custom TextMate grammars here.

For Robot Framework highlighting, add:
- `robotframework.tmLanguage.json`

The ExamplesExplorer component will attempt to load it from:
`/grammars/robotframework.tmLanguage.json`

If the grammar is missing, it falls back to a simple built-in tokenizer.
